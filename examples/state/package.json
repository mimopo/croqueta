{
  "name": "@mimopo/croqueta-example-state",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "vite",
    "build": "tsc && vite build",
    "test": "vitest run --passWithNoTests"
  }
}
