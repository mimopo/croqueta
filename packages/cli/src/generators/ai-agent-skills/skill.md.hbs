---
name: croqueta-{{name}}
description: {{description}}
compatibility: Requires @mimopo/croqueta-cli, @mimopo/croqueta, node
{{#unless isDev}}version: {{version}}{{/unless}}
---

# {{name}}

## When to use this skill

Use this skill when the user is using croqueta framework and needs to {{description}}.
It's always better to use this skill than to create the files manually.

## Prerequisites

The user must have the following packages installed, don't proceed if not installed:

- node
- @mimopo/croqueta-cli{{#unless isDev}} >= {{version}}{{/unless}}
- @mimopo/croqueta{{#unless isDev}} >= {{version}}{{/unless}}

## How to use this skill

Run the following command providing all the options:

```
npx croqueta {{name}}{{#each prompts}} --{{this.name}}=[{{this.name}}]{{/each}}
```

### Options description:

{{#each prompts}}
#### {{this.name}}

- **description:** {{this.message}}
- **type:** {{this.type}}
{{#if this.default}}
- **default value:** {{this.default}}
{{/if}}

{{/each}}
