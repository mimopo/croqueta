import { defineConfig } from 'vite';

import { viteBaseConfig } from '../../vite.base-config';

export default defineConfig({
  ...viteBaseConfig,
  build: {
    outDir: '../../dist/examples/{{fileName name}}',
    emptyOutDir: true,
  },
});
